# 08-成果管理与内部受控复用策略

## 执行说明

<fixed>
本文件负责生成第四章 `（二）项目建设方案` 下的第四个三级项。

执行顺序固定为：
1. 先读取根目录 `README.md`
2. 再读取 `writing-rules/00-总流程与资料边界.md`
3. 再读取 `writing-rules/01-公文文风与句式规则.md`
4. 再读取 `writing-rules/02-编号与结构规则.md`
5. 再读取 `writing-rules/03-深度写作与证据消费规则.md`
6. 再读取 `writing-rules/ch04-项目建设方案-总则.md`
7. 再读取 `writing-rules/ch04-08-成果管理与内部受控复用策略.md`
8. 再读取 `chapter-prompts/ch04-项目建设方案/README.md`
9. 再读取当前文件
10. 最后读取 `reference-materials/<project-slug>/` 下与第四章有关的全部资料

若当前任务是整本生成，且第四章目录 README 与 `writing-rules/00-03`、`writing-rules/ch04-项目建设方案-总则.md` 已在当前轮次完成预读，则进入本文件时只需重新执行第 7 至 10 步。

正式输出位置：`generated-drafts/<project-slug>/ch04-项目建设方案/08-成果管理与内部受控复用策略.md`

本文件只允许输出以下标题：
4. 成果管理与内部受控复用策略

本文件同时受 `writing-rules/ch04-项目建设方案-总则.md` 与 `writing-rules/ch04-08-成果管理与内部受控复用策略.md` 约束。
若出现 `<research>` 区块且用户未明确禁止外部调研，必须先调用 `miro-google-search` 做检索与抓取，再回写正文；不得跳过检索直接凭常识扩写，也不得改用其他搜索通道。
</fixed>

## 固定骨架

<fixed>
正式输出中的 Markdown 标题只能按以下形式出现：

- `### 4. 成果管理与内部受控复用策略`
- `#### （1）成果分级管理`
- `#### （2）内部受控复用边界`
- `#### （3）知识产权与保密`
- `#### （4）对外发布规则`
- `#### （5）管理闭环`

执行要求：
- `### 4. 成果管理与内部受控复用策略` 必须原样输出。
- 本节必须形成“成果形成 -> 分类管理 -> 受控使用 -> 对外边界 -> 归档追踪”的原则性闭环，不得只列口号条款。
- 若资料未明确审批主体、协议文本、备案要求和发布权限层级，只能写抽象控制节点，不得补成具体部门流转。
- 若项目明确不做开源，正文和图都不得擅自引入开源路径。
</fixed>

## 本节图表要求

<fixed>
本文件必须包含 `图4-9 成果管理与受控复用流程图`。

位置要求：
- 放在“成果分级管理 -> 受控复用边界 -> 知识产权与保密 -> 对外发布规则”的总体说明之后。

图示用途：
- 展示成果形成、分类、审批、内部共享、外部发布、归档追踪的闭环流程。

执行要求：
- 若项目不做开源，图中应体现内部受控复用、审批流和敏感信息隔离，不要强行画开源流程。
- 图后必须解释每个节点的管理动作和控制点。
- 图必须直接以内联 Mermaid 代码块写在正文里，不得只在文末补图题和图解。
- 若资料不足，图中的流程节点只能使用“申请/审核/使用/归档/发布边界”等抽象表达，不得自行填入具体协议签订、备案层级、审计部门和权限岗位名称。
</fixed>

## 正式正文结构

### 4. 成果管理与内部受控复用策略
#### （1）成果分级管理
#### （2）内部受控复用边界
#### （3）知识产权与保密
#### （4）对外发布规则
#### （5）管理闭环

<research>
先用 `reference-materials/<project-slug>/` 核对成果归属、成果分级、知识产权、内部共享边界、敏感数据管理、受控复用机制和外部发布审批规则。
本节允许外部调研的范围仅限：
- 保密项目、内部研发项目和行业技术项目在成果管理上的一般原则；
- 知识产权保护、保密承诺、内部共享和对外发布控制的通用边界；
- “受控复用”在行业中的常见原则性表达，例如最小授权、范围限定、敏感信息隔离、可追溯管理等；
- 成果管理闭环图、原则性流程图和分级管理框架的通用表达方式。
本节不允许用外部调研补写本项目自己的审批链、协议文本、备案要求、发布权限层级、权益分配和内部制度细节。
若资料不足，只能保留到“原则、边界、待补充”层面，不得把通用制度想象成项目既有规则。
若进行了外部调研，必须先把可消费结论整理到 `reference-materials/<project-slug>/external-research-notes.md` 的“第四章 项目建设方案”部分，再回写正文。
</research>

<write>
围绕“成果分级管理 -> 受控复用边界 -> 知识产权与保密 -> 对外发布规则”展开。

要求：
- 若项目明确不做开源，只写内部受控复用，不要强行写开源。
- 若项目允许部分开放，要写清开放范围、审批要求和敏感信息边界。
- 本节要体现管理闭环，而不是只列原则。
- `图4-9` 只允许画到资料能支撑的原则性流程节点；若资料没有明确审批主体和协议文本，不得自行写出具体审批部门、协议签订节点和备案层级。
- 外部调研只能帮助说明“这类项目通常为什么要做成果分级、受控复用和知识产权保护”，不得把通用制度套写成“本项目已生效制度”。
</write>
