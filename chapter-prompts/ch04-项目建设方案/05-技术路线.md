# 05-技术路线

## 执行说明

<fixed>
本文件负责生成第四章 `（二）项目建设方案` 的开头部分。

执行顺序固定为：
1. 先读取根目录 `README.md`
2. 再读取 `writing-rules/00-总流程与资料边界.md`
3. 再读取 `writing-rules/01-公文文风与句式规则.md`
4. 再读取 `writing-rules/02-编号与结构规则.md`
5. 再读取 `writing-rules/03-深度写作与证据消费规则.md`
6. 再读取 `writing-rules/ch04-项目建设方案-总则.md`
7. 再读取 `writing-rules/ch04-05-技术路线.md`
8. 再读取 `chapter-prompts/ch04-项目建设方案/README.md`
9. 再读取当前文件
10. 最后读取 `reference-materials/<project-slug>/` 下与第四章有关的全部资料

正式输出位置：`generated-drafts/<project-slug>/ch04-项目建设方案/05-技术路线.md`

本文件必须输出以下标题：
（二）项目建设方案
1. 技术路线

本文件同时受 `writing-rules/ch04-项目建设方案-总则.md` 与 `writing-rules/ch04-05-技术路线.md` 约束。
若出现 `<research>` 区块且用户未明确禁止外部调研，必须先调用 `miro-google-search` 做检索与抓取，再回写正文；不得跳过检索直接凭常识扩写，也不得改用其他搜索通道。
</fixed>

## 固定骨架

<fixed>
正式输出中的 Markdown 标题只能按以下形式出现：

- `## （二）项目建设方案`
- `### 1. 技术路线`
- `#### （1）总体路线说明`
- `#### （2）技术架构`
- `#### （3）数据流与业务流`
- `#### （4）关键模块与接口`
- `#### （5）测试验证安排`
- `#### （6）工程应用路径`

执行要求：
- `## （二）项目建设方案` 必须由本文件输出，且只能出现 1 次。
- `### 1. 技术路线` 必须原样输出，不得写成新的 `#` 一级标题。
- 其余细分内容若需要展开，只能用 `####` 或正文中的加粗引导语。
</fixed>

## 本节图表要求

<fixed>
本文件必须包含 `图4-5 项目整体技术路线图`。

位置要求：
- 放在技术路线总述之后、分层展开之前。

图示用途：
- 展示数据层、算法层、服务层、平台层、验证层之间的关系，或展示“输入 -> 处理 -> 输出 -> 验证”的整体路线。

执行要求：
- 图中各层级、模块、任务名称要与正文保持一致。
- 图后必须有解释性文字，说明每一层承担的作用，以及与 `2. 项目解决的主要问题`、`3. 项目研发内容` 的对应关系。
- 若项目存在多个子路线，可在同一图中分泳道或分区展示，不另起新的主图编号。
- 图必须直接以内联 Mermaid 代码块落在“总体路线说明”之后，不得只写图题和解释。
</fixed>

## 正式正文结构

## （二）项目建设方案
### 1. 技术路线
#### （1）总体路线说明
#### （2）技术架构
#### （3）数据流与业务流
#### （4）关键模块与接口
#### （5）测试验证安排
#### （6）工程应用路径

<research>
本节原则上只允许使用 `reference-materials/<project-slug>/` 中的技术架构、数据流、业务流、算法层、平台层、验证层、关键接口和关键图表需求。
若需补充，只允许补充公开方法术语和通用架构名词，不得新增项目级模块、接口规范或实施阶段。
</research>

<write>
本文件只写以下内容：
- `（二）项目建设方案`
- `1. 技术路线`

要求：
- 先给出总体路线，再分层写清数据、算法、服务、平台、验证等逻辑。
- 技术路线必须与前面的“主要问题”和“研发内容”一一对应。
- 若存在图示或表格需求，要在正文中自然引出。
</write>
